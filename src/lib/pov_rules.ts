/**
 * src/lib/pov_rules.ts — re-export shim
 *
 * 실제 구현은 src/policies/pov_rules.ts로 이동했다.
 * 기존 import 경로 호환성을 위해 이 shim을 유지한다.
 * 새 코드는 src/policies/pov_rules.ts 또는 src/policies/index.ts를 직접 임포트할 것.
 */
export * from "../policies/pov_rules.js";
