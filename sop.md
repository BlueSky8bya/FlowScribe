# Final Project SOP: FlowScribe
## Persona-Sync Novel Platform
**Version 3.0 — Final SOP**

## 0. 문서 역할 및 계층 (Document Role & Hierarchy)

이 문서는 **FlowScribe의 상위 정본(master specification)** 이다.

역할 구분은 다음과 같다.

- `claude.md`: 에이전트의 초기 판단, 최소 규칙, 문서 라우팅 담당
- `SOP.md`: 제품 철학, 아키텍처, 개인화 구조, 정책, 스키마를 포함한 상위 정본
- 각 폴더 내부의 전용 `.md` 문서: 해당 영역의 실행용 세부 지침 문서

즉, `claude.md`는 항상 처음 읽는 최소 문서이고,  
`SOP.md`는 FlowScribe 전체 설계의 기준 문서이며,  
실제 구현과 실행 시에는 필요한 경우에만 각 폴더 내부의 세부 `.md` 문서를 추가로 읽는다.

이 문서는 가능한 한 상세하게 유지하되,  
루트 라우팅 문서의 역할을 대신하지 않는다.

---

## 1. 프로젝트 개요 (Vision)

**FlowScribe**는 사용자의 독서 습관, 언어 반응, 감정적 선호를 회차별로 누적 학습하여, 텍스트 출력 속도, 문장 호흡, TTS, BGM 연출을 점차 사용자에게 맞춰가는 **적응형 숏폼 문학 플랫폼**이다.

이 프로젝트의 목적은 심리 측정을 위한 연구 시스템이 아니라, **읽을수록 독자와 호흡이 맞아가는 재미 중심의 개인화 문학 서비스**를 만드는 데 있다.

즉, FlowScribe는 단순히 이야기를 생성하는 플랫폼이 아니라, **사용자마다 다른 읽기 리듬과 몰입 방식을 학습하여 서사 경험 자체를 조율하는 플랫폼**을 지향한다.

---

## 2. 핵심 제품 목표 (Product Goal)

FlowScribe의 목표는 다음 세 가지이다.

### 2.1 개인화된 서사 경험 제공
사용자별 읽기 속도, 대사 선호, 긴장감 반응, 감성 호흡을 반영해 텍스트를 조정한다.

### 2.2 회차 누적형 적응 시스템 구축
1화, 2화, 10화, 1권, 2권이 쌓일수록 사용자 로그를 기반으로 개인화 가중치를 업데이트한다.

### 2.3 텍스트-음성-사운드의 동기화
문장 리듬, TTS 낭독, 배경음이 서로 따로 노는 것이 아니라, 하나의 장면처럼 작동하도록 설계한다.

---

## 3. 서비스 정체성 (Project Identity)

FlowScribe는 다음 원칙을 중심으로 설계한다.

- 처음부터 완벽히 사용자를 이해하는 시스템이 아니다.
- 대신 **사용자가 읽을수록 더 잘 맞아가는 시스템**이다.
- 개인화의 대상은 모델 본체가 아니라, **사용자별 외부 개인화 계층**이다.
- LLM은 고정된 베이스 모델로 유지하고, 사용자의 반응 데이터를 기반으로 **개인화 상태 벡터**를 계속 업데이트한다.
- 이 프로젝트의 핵심은 "정확한 심리측정"이 아니라, **독자와 작품의 호흡이 점차 맞아가는 경험**을 제공하는 데 있다.

---

## 4. 핵심 아키텍처 원칙 (Core Design Principles)

### 4.1 베이스 모델 고정 원칙
텍스트 생성용 LLM은 매 회차마다 바꾸지 않고, **하나의 주력 모델을 고정**한다.

- **Main Text Generation Model (로컬):** `gemma4` — `unsloth/gemma-4-E4B-it-Q4_K_M` (GGUF), RTX 3080 CUDA, Ollama 로컬 서버 (`http://localhost:11434/v1`)
- **외부 API 전환 옵션:** `.env`의 `DEEPSEEK_BASE_URL`, `DEEPSEEK_MODEL`, `DEEPSEEK_API_KEY`를 교체하면 DeepSeek-V3.2 등 외부 API로 즉시 전환 가능

선정 이유:
- 로컬 추론으로 API 비용 없이 빠른 반복 개발이 가능하다.
- OpenAI 호환 엔드포인트를 통해 SDK 변경 없이 모델 교체가 가능하다.
- 개인화는 모델 내부 미세조정이 아니라 외부 가중치 레이어로 처리할 수 있다.

### 4.2 개인화는 외부 계층에서 수행
사용자 맞춤화는 모델 파인튜닝이 아니라, 다음과 같은 사용자 상태 객체를 별도 저장·갱신하는 방식으로 구현한다.

- `ReaderProfile_static`
- `ReaderProfile_dynamic`
- `GenreProfile`
- `SessionPacing`

이 구조를 통해:
- 비용을 낮추고
- 롤백을 쉽게 하며
- 장르별 편향을 분리 저장하고
- 책 수가 늘어날수록 누적 학습이 자연스럽게 이루어지도록 한다

### 4.3 사용자 경험 우선 원칙
모든 기술 요소는 "정교함"보다 **몰입감과 재미**를 우선한다.  
즉, 설명 가능한 복잡한 모형보다 실제로 읽는 맛이 좋아지는 구성이 우선이다.

---

## 4.4 문서 분화 원칙 (Spec Decomposition Policy)

이 SOP는 상위 정본으로 유지하며, 실제 실행 시 필요한 세부 규칙은 각 폴더 내부의 전용 `.md` 문서로 분화한다.

분화 원칙은 다음과 같다.

- 루트 `claude.md`에는 최소 규칙만 둔다.
- 상세 정책, 계산 규칙, 예외 처리, 구현 세부는 각 폴더 내부 문서로 내린다.
- 에이전트는 항상 이 SOP 전체를 반복해서 읽는 대신, 필요한 경우 관련 폴더의 세부 문서만 추가로 읽는다.
- 하위 문서는 이 SOP와 충돌해서는 안 되며, 이 문서의 내용을 구체화하는 방향으로 작성한다.

---

## 4.5 섹션 소유 영역 매핑 (Section Ownership Mapping)

이 SOP의 주요 섹션은 아래와 같이 하위 폴더 문서로 분화할 수 있다.

- 5. 개인화 학습 구조  
- 6. 개인화 업데이트 방식  
- 7. 개인화에 사용되는 로그 데이터  
- 8. 독서 성향 6요소  
- 9. 초기 보정 시스템  
  -> `profiles/`

- 10. 텍스트 호흡 예측 엔진  
- 12. Director Mode  
- 14. Visual Pacing Engine  
  -> `workflows/` 및 `tools/`

- 11. 생성 설정 시스템  
- 13. 메모리 구조  
  -> `story_data/` 및 `workflows/`

- 15. 오디오 및 TTS 시스템  
- 16. Personal Voice Archive 정책  
  -> `assets/`, `workflows/`, 필요 시 `profiles/`

- 18. 데이터 저장 구조  
  -> `profiles/`, `logs/`, `story_data/`, `assets/`

- 20. MVP 범위  
  -> `workflows/`

이 매핑은 실제 구현 시 세부 문서를 분리하기 위한 기준이며,  
에이전트가 처음부터 모든 문서를 한꺼번에 읽도록 만들기 위한 구조가 아니다.

---

## 4.6 규범 수준 표기 원칙 (Normative Level Policy)

이 SOP의 각 항목은 필요에 따라 아래 세 수준으로 해석한다.

- **Normative**: 반드시 지켜야 하는 규칙
- **Configurable**: 프로젝트 상황에 따라 조정 가능한 항목
- **Implementation Detail**: 하위 문서 또는 코드 레벨에서 구체화할 항목

기본 해석 원칙은 다음과 같다.

- 제품 철학, 개인화 방향, Director Mode 우선순위, Voice Archive 동의 원칙은 **Normative**로 본다.
- 하이퍼파라미터, 점수 범위, 세부 저장 형식, 로그 수집 방식 일부는 **Configurable**로 본다.
- 실제 함수 구조, API 연동 방식, 스키마 세부 컬럼 확장, 임시 처리 방식은 **Implementation Detail**로 본다.

하위 `.md` 문서 작성 시에는,  
이 문서의 Normative 항목을 깨지 않는 범위에서 Configurable 및 Implementation Detail을 구체화한다.

---

## 5. 개인화 학습 구조 (Personalization Learning Structure)

### 5.1 `ReaderProfile_static`
비교적 천천히 변하는 장기 성향 벡터

예시:
- 대화체 선호도
- 문장 복잡성 수용도
- 감정 묘사 선호도
- 긴장감 선호도
- 설명문 허용도
- 오디오 연출 허용도

### 5.2 `ReaderProfile_dynamic`
최근 몇 화 동안의 반응을 반영하는 단기 상태 벡터

예시:
- 최근 몰입도 변화
- 현재 선호 속도
- 최근 이탈 구간 특성
- 피로도 추정
- 최근 대사/설명문 반응 차이

### 5.3 `GenreProfile`
장르별 편향 저장용 프로필

예시:
- 스릴러: 짧은 문장, 빠른 템포, 높은 긴박감
- 로맨스: 대사 비중 증가, 감정선 강조
- 판타지: 세계관 설명 허용치 증가
- 일상물: 낮은 갈등 강도, 잔잔한 흐름 선호

### 5.4 `SessionPacing`
현재 화 또는 현재 세션에만 적용되는 즉시성 파라미터

예시:
- 현재 출력 속도
- 문장 사이 대기 시간
- 대사 출력 템포
- 시각 효과 강도
- 현재 TTS 재생 속도
- 장면 전환 지연값

---

## 6. 개인화 업데이트 방식 (Weight Update Policy)

사용자별 가중치는 회차 종료 후 행동 로그를 반영해 점진적으로 업데이트한다.

기본 업데이트 식:

`w_next = (1 - alpha_t) * w_t + alpha_t * w_hat_t`

설명:
- `w_t`: 현재 사용자 가중치
- `w_hat_t`: 이번 화에서 관측된 선호 추정치
- `alpha_t`: 학습률

운영 원칙:
- 초반에는 학습률을 높여 빠르게 적응한다.
- 회차가 누적될수록 학습률을 낮춰 안정성을 높인다.
- 최근 반응은 `ReaderProfile_dynamic`에 우선 반영한다.
- 반복적으로 확인된 패턴만 `ReaderProfile_static`에 천천히 반영한다.
- 장르 특이 반응은 `GenreProfile`에 별도 저장한다.

즉, 사용자가 책을 많이 읽을수록 LLM이 바뀌는 것이 아니라, **사용자 프로필이 점차 정교해지는 구조**를 채택한다.

---

## 7. 개인화에 사용되는 로그 데이터 (Behavior Signal Inputs)

FlowScribe는 명시적 설문보다 **행동 로그를 더 강한 신호**로 본다.

주요 수집 항목:
- 문단별 체류 시간
- 자동 출력 중 이탈 지점
- 되감기 구간
- 수동 속도 조절 횟수
- 특정 문단 반복 청취
- `TTS On/Off` 빈도
- `BGM` 음소거 빈도
- 화 완독률
- 다음 화 진입까지 걸린 시간
- 특정 등장인물 장면에서의 체류 변화
- 특정 감정 장면에서의 반응 선택
- `Scene skip` 또는 `pause` 빈도

이 로그를 기반으로 사용자 선호를 추정하고, 다음 화 생성과 연출에 반영한다.

---

## 8. 독서 성향 6요소 (Reading Hexagon)

사용자 독서 성향은 다음 6요소로 관리하며, 각 점수는 `0~100` 범위에서 지속적으로 갱신한다.

1. `Focus` — 몰입도
2. `Sentiment` — 감성 반응
3. `Urgency` — 긴박감 선호
4. `Complexity` — 복잡성 수용도
5. `Dialogue` — 대화 선호
6. `Audio-Sync` — 음향 동기화 선호

이 값들은 고정 성격 지표가 아니라, 회차별 로그를 바탕으로 계속 보정되는 **가변 상태 벡터**이다.

---

## 9. 초기 보정 시스템 (Initial Calibration)

회원가입 직후 또는 첫 작품 시작 전, 짧은 설정 과정을 통해 기본값을 설정한다.

### 9.1 초기 입력
- 선택 장르
- 선호 분위기
- 갈등 강도 슬라이더
- 복선 빈도 슬라이더
- 기본 문체 선택
- `TTS` 사용 여부
- `BGM` 사용 여부

### 9.2 초기 속도 보정
사용자가 `3~5줄` 분량의 짧은 텍스트를 읽는 동안 다음을 추정한다.

- 기본 읽기 속도 `V0`
- 줄바꿈 반응
- 대사/서술문 선호 차이
- 문장 길이에 대한 처리 속도

초기 보정은 출발점일 뿐이며, 실제 개인화는 이후 회차 누적 로그를 기반으로 정교화한다.

---

## 10. 텍스트 호흡 예측 엔진 (Reading Pace Prediction Engine)

각 문단 또는 문장 블록별 예상 소화 시간을 계산하고, 이에 따라 시각 출력과 `TTS` 타이밍을 조정한다.

### 10.1 주요 독립 변수

#### 부호 기반 가중치
- `w_dot`: 마침표
- `w_comma`: 쉼표
- `w_quest`: 물음표
- `w_excl`: 느낌표
- `w_tilde`: 물결표
- `w_elli`: 생략부호

#### 구조 기반 가중치
- `w_dquote`: 큰따옴표
- `w_squote`: 작은따옴표
- `w_space`: 공백
- `w_indent`: 들여쓰기
- `w_newline1`: 1회 줄바꿈
- `w_newlineN`: 다중 줄바꿈

#### 인지 부하 보정치
- 현재 화의 등장인물 수
- 대명사 빈도
- 장면 전환 횟수
- 문장 길이 분산
- 세계관 고유명사 밀도

### 10.2 예측 목적
이 엔진은 "사용자가 이 문장을 정확히 몇 초 만에 읽는가"를 맞히기 위한 것이 아니라, **사용자에게 더 잘 맞는 출력 리듬을 추정하는 것**을 목적으로 한다.

---

## 11. 생성 설정 시스템 (Story Generation Setup)

작품 시작 시 다음 요소를 설정한다.

### 11.1 기본 설정
- 장르
- 등장인물 정보
  - 이름
  - 외형적 특성
  - 성격
  - 역할
- 서술 시점
- 문체
- 회차 길이
- 전체 화 수 범위

### 11.2 가변 분량 시스템
사용자가 설정한 `N ± X` 범위 안에서, 에이전트가 서사 밀도와 반응 로그를 고려하여 내부적으로 최적 결말 지점을 정한다.

즉, 결말은 기계적으로 고정되지 않고, **기승전결 밀도와 사용자의 반응을 함께 고려해 유동적으로 조정**된다.

### 11.3 사용자 제어 슬라이더
- 갈등 강도: `1~10`
- 복선 빈도: `1~10`
- 감정 묘사 비중: `1~10`
- 대사 비중: `1~10`
- 연출 강도: `1~10`

---

## 12. Director Mode (사용자 개입 우선 모드)

사용자의 중간 개입은 FlowScribe의 핵심 기능으로 간주한다.

예시:
- "이 캐릭터를 더 차갑게 바꿔줘"
- "이번부터 분위기를 더 어둡게"
- "복선을 줄이고 감정선을 늘려줘"
- "주인공 말투를 더 담백하게 해줘"

### 12.1 적용 원칙
Director Mode는 높은 우선순위를 갖지만, 무조건 서사를 파괴하지 않는다.

원칙:
1. 세계관의 핵심 규칙은 유지한다.
2. 기존 복선은 삭제보다 재맥락화를 우선한다.
3. 즉시 적용 가능한 항목은 다음 화부터 반영한다.
4. 장기 구조 수정은 점진적으로 반영한다.

즉, 사용자 개입은 서사의 방향을 바꿀 수 있지만, 이야기 전체를 붕괴시키지 않는 선에서 재해석된다.

---

## 13. 메모리 구조 (Narrative Memory Architecture)

### 13.1 `World Bible`
변하지 않는 핵심 설정 저장
- 세계관 규칙
- 캐릭터 기본값
- 고정 관계도
- 장르 톤
- 금지 설정

### 13.2 `Rolling Summary`
최근 `3~10화` 정도의 단기 요약
- 직전 사건
- 감정선 변화
- 진행 중인 갈등
- 최근 연출 스타일

### 13.3 `Long-term Memory / Foreshadow Memory`
장기 복선 및 중요 설정 저장
- 회수 예정 떡밥
- 숨겨진 인물 정보
- 사용자가 좋아한 장면 유형
- 회차별 선택 패턴
- 장기 플롯 분기 정보

---

## 14. Visual Pacing Engine

문장부호, 줄바꿈, 긴장감 정도에 따라 시각적 출력 템포를 동적으로 제어한다.

예시:
- `...`: 출력 지연 증가 + 페이드 지연
- `!`: 짧은 진동 또는 강한 전환 효과
- 긴 공백 및 다중 줄바꿈: 정적 연출 강화
- 빠른 대사 연속 구간: 출력 속도 증가
- 심리 묘사 구간: 단어 간 호흡 증가

목적은 단순 애니메이션이 아니라, **텍스트가 장면처럼 느껴지게 만드는 연출 엔진**을 구성하는 데 있다.

---

## 15. 오디오 및 TTS 시스템 (Audio & TTS)

### 15.1 감정 기반 TTS
문단별 감정 태그를 바탕으로 음성 파라미터를 조정한다.

조정 대상:
- `Rate`
- `Pitch`
- `Pause Length`
- `Emphasis`
- `Breath` 느낌

또한 FlowScribe는 기본 TTS 엔진과 별도로, **플랫폼 내부 음성 튜닝 시스템을 통해 생성된 사용자 맞춤 음성**을 캐릭터 보이스로 매핑할 수 있다.

### 15.2 Spatial Audio
복수 화자 대화 장면에서 좌우 밸런스를 조절해 장면감을 강화한다.

예시:
- 좌측 인물 발화 -> `L` 비중 증가
- 우측 인물 발화 -> `R` 비중 증가
- 회상/내면 독백 -> 중앙 또는 리버브 처리

### 15.3 BGM 동기화
초기 버전에서는 생성형 `BGM`보다 **무료 라이브러리 기반 벡터 매칭 방식**을 우선 사용한다.

이유:
- 비용 절감
- 안정적 품질 확보
- 장면 태그 기반 빠른 매칭 가능

장면 태그 예시:
- `melancholy`
- `suspense`
- `soft romance`
- `slice of life`
- `solitude`
- `confrontation`

---

## 16. Personal Voice Archive 정책

FlowScribe는 외부 웹사이트에 등록된 음성을 가져와 사용하는 구조가 아니라, **프로젝트 내부에 구축된 음성 튜닝 시스템을 통해 사용자가 자신의 목소리를 직접 학습·생성하고, 이를 Voice Archive에 등록하는 방식**을 채택한다.

이 아카이브에 등록된 목소리는 등록자의 허용 범위에 따라 본인만 사용하거나, 다른 사용자들도 캐릭터 음성으로 사용할 수 있다.

### 16.1 허용 조건
`Voice Archive`에 등록 가능한 음성은 다음 조건을 만족해야 한다.

- **FlowScribe 내부 음성 튜닝 시스템**을 통해 생성된 음성일 것
- 해당 음성의 원본 제공자가 자신의 목소리 사용 및 학습에 **직접 동의**했을 것
- 등록 시 사용 범위(`본인 전용 / 공유 허용 / 공개 허용`)를 명시했을 것
- 타인의 음성을 무단 업로드하거나, 본인 동의 없이 학습한 음성이 아닐 것

즉, `Voice Archive`의 모든 음성은 **FlowScribe 내부에서 직접 생성되고, 명시적 동의를 거쳐 등록된 자산만 허용**한다.

### 16.2 운영 원칙
모든 음성 자산에는 다음 메타데이터를 저장한다.

- 음성 소유자 ID
- 생성 일시
- 원본 음성 제공자 동의 여부
- 사용 범위
- 캐릭터 음성 매핑 허용 여부
- 상업적 사용 허용 여부
- 재공유 허용 여부
- 비활성화 상태
- 삭제 요청 상태

### 16.3 사용 범위 레벨
`Voice Archive` 음성은 최소 3단계 권한으로 구분한다.

- **Private**: 본인만 사용 가능
- **Shared**: 다른 사용자가 열람·선택 가능하나, 플랫폼 내부 용도로만 사용 가능
- **Public**: 공개 음성 라이브러리로 제공 가능

필요 시 다음과 같은 세부 제한도 둘 수 있다.
- 성인 콘텐츠 사용 불가
- 상업 콘텐츠 사용 불가
- 특정 장르에서만 사용 가능
- AI 낭독 전용
- 보이스팩 재배포 불가

### 16.4 필수 기능
시스템에는 다음 기능이 반드시 포함되어야 한다.

- 음성 등록 전 명시적 동의 절차
- 등록 후 사용 범위 수정 기능
- 음성 비활성화 기능
- 삭제 요청 즉시 반영 기능
- 이미 공유된 음성의 사용 이력 추적 기능

### 16.5 플랫폼 해석
`Personal Voice Archive`는 단순한 음성 저장소가 아니라, **사용자가 직접 만든 음성 페르소나를 등록하고, 공유 범위까지 통제할 수 있는 동의 기반 음성 자산 시스템**으로 정의한다.

---

## 17. 기술 스택 정책 (Implementation Policy)

### 17.1 LLM
- `DeepSeek-V3.2` 고정 채택
- 장문 서사 생성과 문체 일관성 유지 담당

### 17.2 TTS
- 내부 음성 튜닝 시스템 기반 구조 채택
- 사용자 동의 기반 보이스 생성 및 아카이브 등록 지원
- `Voice Archive` 권한 범위에 따라 캐릭터 보이스 공유 가능

### 17.3 BGM
- 무료 라이브러리 우선 사용
- 장면 태그 기반 검색 및 추천
- 추후 생성형 `BGM`으로 확장 가능

### 17.4 처리 방식
- 모든 주요 로직은 비동기 처리
- 텍스트 생성, 로그 저장, 개인화 업데이트, 오디오 연동을 분리 큐로 관리
- 개인정보 및 음성 데이터는 암호화 저장

---

## 18. 데이터 저장 구조 (Suggested Data Schema)

### 18.1 `User`
- `user_id`
- `genre_preferences`
- `tts_enabled`
- `bgm_enabled`
- `created_at`

### 18.2 `Reader Profile`
- `user_id`
- `static_vector`
- `dynamic_vector`
- `genre_vector_map`
- `last_updated`

### 18.3 `Session Log`
실제 DB 스키마 (`session_logs` 테이블):
- `id` (UUID PK)
- `book_id` (TEXT NOT NULL) — user_id 대신 book 단위로 로그 귀속
- `episode_number` (INT NOT NULL) — episode_id 대신 정수형 회차
- `dwell_ms` (INT) — 체류 시간 ms 단위
- `dropout_position` (FLOAT) — 이탈 위치 비율 (0.0~1.0)
- `rewind_count` (INT)
- `speed_changes` (INT)
- `completion_rate` (FLOAT)
- `emotion_signals` (JSONB)
- `created_at` (TIMESTAMPTZ)

> **Note:** 문단 단위(`paragraph_id`) 분리는 Phase 2 이후 구현. 현재는 화 단위 집계.

### 18.4 `Story State`
- `book_id`
- `episode_id`
- `world_bible_ref`
- `rolling_summary`
- `foreshadow_memory`
- `director_overrides`

### 18.5 `Voice Archive`
- `voice_id`
- `owner_id`
- `consent_status`
- `scope_level`
- `allowed_usage`
- `share_status`
- `commercial_use_allowed`
- `active_status`
- `deleted_at`
- `created_at`

### 18.6 `Voice Training Metadata`
- `voice_id`
- `training_session_id`
- `source_owner_id`
- `consent_version`
- `model_version`
- `tuning_config`
- `audit_log_ref`

---

## 19. UI/UX 원칙

FlowScribe의 UI는 다음 방향을 지향한다.

- 아날로그 감성과 디지털 정밀함의 조화
- 과도한 기능 노출보다 장면 몰입 중심
- 미니멀하지만 감정선이 느껴지는 인터페이스
- 조작보다 감상 흐름을 방해하지 않는 화면 구조

핵심 원칙은 다음과 같다.

- 사용자는 설정창을 다루는 느낌보다, **이야기와 호흡을 맞추는 느낌**을 받아야 한다.
- 개인화는 조용히 작동해야 하며, 사용자가 피로를 느끼지 않도록 해야 한다.

---

## 20. MVP 범위 (추천 구현 우선순위)

### Phase 1
- 텍스트 생성
- 회차 저장
- 기본 `Reader Profile` 구축
- 문장 호흡 기반 `Visual Pacing`
- 행동 로그 수집

### Phase 2
- `TTS` 감정 반영
- `SessionPacing` 실시간 조정
- `Director Mode` 반영
- 장르별 프로필 누적

### Phase 3
- `BGM` 자동 매칭
- `Spatial Audio`
- `Voice Archive` 연동
- 장기 복선 메모리 고도화

### Phase 4
- 내부 음성 튜닝 시스템 고도화
- `Voice Archive` 공유 생태계 확장
- 사용자 제작 음성 페르소나 추천 기능
- 캐릭터별 보이스 스타일 자동 매핑

---

## 21. 최종 정의 (Final Definition)

FlowScribe는 단순한 AI 소설 생성기가 아니다.  
이 프로젝트는 사용자가 읽을수록 그 사람의 리듬, 감정선, 몰입 방식을 학습하여, **텍스트와 오디오, 연출의 호흡을 점차 사용자에게 맞춰가는 적응형 문학 플랫폼**이다.

핵심은 "처음부터 완벽한 맞춤"이 아니라, **회차가 쌓일수록 독자와 작품이 서로 길들여지는 경험**을 만드는 것이다.

또한 `Personal Voice Archive`를 통해, 사용자는 플랫폼 내부 음성 튜닝 시스템에서 직접 만든 자신의 목소리를 등록하고, 그 허용 범위 안에서 다른 사용자들과 공유할 수 있다.

즉, FlowScribe는 아래 세 요소를 결합한 차세대 숏폼 문학 플랫폼으로 정의한다.

- 개인화된 독서 경험
- 적응형 서사 생성
- 동의 기반 음성 자산 생태계